import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['hebbkx1anhila5yf.public.blob.vercel-storage.com'],
  },
  // 다른 Next.js 설정도 여기에 포함할 수 있습니다
};

export default nextConfig;
